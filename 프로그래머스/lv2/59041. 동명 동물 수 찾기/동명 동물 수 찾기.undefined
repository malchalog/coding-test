-- 코드를 입력하세요
SELECT name
      ,COUNT(name) AS COUNT 
FROM animal_ins
WHERE name IS NOT NULL
GROUP BY name
HAVING COUNT(name) > 1
ORDER BY name



/*SELECT NAME
      ,count(NAME) AS "COUNT"
FROM ANIMAL_INS
GROUP BY NAME
HAVING COUNT(NAME)>=2
ORDER BY NAME*/